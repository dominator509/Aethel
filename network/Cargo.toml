[package]
name = "network"
version = "0.1.0"
edition = "2021"

[dependencies]
tokio = { version = "1.0", features = ["full"] }
quinn = "0.11"
rustls = "0.23"
sha2 = "0.10"
crypto = { path = "../crypto" }
